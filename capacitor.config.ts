import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'route-navigation-gmaps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
